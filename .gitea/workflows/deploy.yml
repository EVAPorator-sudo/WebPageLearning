name: Deploy Website

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3

      - name: Sync website
        run: |
          cp -r ./* /usr/share/nginx/html/
          echo "Website deployed successfully"